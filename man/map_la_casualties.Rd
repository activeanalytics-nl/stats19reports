% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{map_la_casualties}
\alias{map_la_casualties}
\title{Choropleth map of casualty counts by Local Authority}
\usage{
map_la_casualties(
  region_sf,
  variable = c("fatal_cas", "serious_cas", "slight_cas", "total_cas", "ksi_cas"),
  home_LA,
  start_year,
  end_year,
  title = NULL,
  legend_title = NULL,
  palette = "wes.zissou1",
  breaks_style = c("cat", "fixed", "log10_pretty")
)
}
\arguments{
\item{region_sf}{An \code{sf} data frame of LA boundaries with casualty
columns (e.g. from \code{summarise_casualties_per_la()}).}

\item{variable}{Character. Column to plot. One of \code{"fatal_cas"},
\code{"serious_cas"}, \code{"slight_cas"}, \code{"total_cas"},
\code{"ksi_cas"}.}

\item{home_LA}{Character. Local Authority to highlight.}

\item{start_year, end_year}{Integers. Year range to map.}

\item{title}{Character. Map title, or \code{NULL} to auto-generate.}

\item{legend_title}{Character. Legend title, or \code{NULL}.}

\item{palette}{Character. Colour palette name. Default
\code{"wes.zissou1"}.}

\item{breaks_style}{Character. Break style for \code{tm_scale_intervals()}.}
}
\value{
A \code{tmap} object.
}
\description{
Creates a choropleth of casualty totals by Local Authority for a given
severity and year, with an inset listing the top 10 LAs.
}
\examples{
\dontrun{
tm <- map_la_casualties(la_cas_sf, variable = "ksi_cas", year = "2024")
}
}
