% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{map_city_boundary}
\alias{map_city_boundary}
\title{Create a city boundary map with basemap background}
\usage{
map_city_boundary(
  city_shape,
  city_name,
  map_type = c("osm", "carto_light"),
  border_col = "#ff7733",
  plot_dir = "plots/",
  border_width = 9,
  width = 5000,
  height = 5000
)
}
\arguments{
\item{city_shape}{An \code{sf} polygon (or multipolygon) describing the city
boundary. Must have a valid CRS.}

\item{city_name}{Character. Short name used for the output filename and map
title.}

\item{map_type}{Character. One of \code{"osm"} or \code{"carto_light"}. Controls which
basemap service and style to request. Default \code{c("osm", "carto_light")}.}

\item{border_col}{Character. Colour used for the city boundary line.
Default \code{"#ff7733"}.}

\item{plot_dir}{Character. Directory where the PNG will be saved. Created if
it does not exist. Default \code{"plots/"}.}

\item{border_width}{Numeric. Boundary line width.}

\item{width, height}{Integer. Output image dimensions in pixels. Defaults are
5000 x 5000 to match the original behaviour.}
}
\value{
A \code{tmap} object (invisibly). The PNG is written to disk as a side
effect.
}
\description{
\code{map_city_boundary()} renders a basemap clipped to a city polygon and overlays the
city boundary. The function saves a high-resolution PNG to \code{plot_dir} and
returns the \code{tmap} object invisibly.
}
\examples{
\dontrun{
map_city_boundary(city_shape = my_city_sf, city_name = "MyCity")
}
}
