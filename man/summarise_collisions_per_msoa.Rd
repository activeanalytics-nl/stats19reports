% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{summarise_collisions_per_msoa}
\alias{summarise_collisions_per_msoa}
\title{Summarise collisions per MSOA}
\usage{
summarise_collisions_per_msoa(
  casualties,
  crashes,
  per_capita = FALSE,
  by_year = TRUE,
  casualty_types = "All",
  casualty_sexes = c("Male", "Female")
)
}
\arguments{
\item{casualties}{Casualty data frame.}

\item{crashes}{An \code{sf} data frame of crash records.}

\item{per_capita}{Logical. If \code{TRUE}, compute per-1000-population
rates. Default \code{FALSE}.}

\item{by_year}{Logical. If \code{TRUE}, group by year; otherwise collapse
all years to \code{"All"}. Default \code{TRUE}.}

\item{casualty_types}{Character. Casualty type filter, or \code{"All"}.
Default \code{"All"}.}

\item{casualty_sexes}{Character vector of sexes to include. Default
\code{c("Male", "Female")}.}
}
\value{
An \code{sf} data frame with one row per MSOA (per year) and
columns for casualty counts, rankings, and optionally per-capita rates.
}
\description{
Spatially joins crashes to MSOA boundaries and aggregates casualty counts
by severity, year, and MSOA. Adds rankings and optionally computes
per-capita rates. Returns an \code{sf} object with MSOA names from the
House of Commons Library MSOA Names dataset.
}
\examples{
\dontrun{
msoa_coll <- summarise_collisions_per_msoa(casualties, crashes)
msoa_cyc  <- summarise_collisions_per_msoa(casualties, crashes,
                                           casualty_types = "Cyclist",
                                           per_capita = TRUE)
}
}
