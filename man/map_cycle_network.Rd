% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{map_cycle_network}
\alias{map_cycle_network}
\title{Plot cycling network with segregation classification and summary stats}
\usage{
map_cycle_network(
  city,
  city_shape,
  osm_data,
  city_pop,
  title_position = c("right", "left"),
  stats_position = c("right", "left"),
  legend_position = c("left", "right"),
  plot_dir = "plots/",
  dpi = 400
)
}
\arguments{
\item{city}{Character. City name used for title and filename.}

\item{city_shape}{An \code{sf} polygon (or multipolygon) describing the city
boundary.}

\item{osm_data}{OSM data object expected by \code{osmactive::get_driving_network}
and \code{osmactive::get_cycling_network}.}

\item{city_pop}{Numeric. Population used in the summary statistics.}

\item{title_position}{Character. \code{"left"} or \code{"right"} placement for the map
title. Default \code{"right"}.}

\item{stats_position}{Character. \code{"left"} or \code{"right"} placement for the
credits/stats box. Default \code{"right"}.}

\item{legend_position}{Character. \code{"left"} or \code{"right"} placement for the
legend. Default \code{"left"}.}

\item{plot_dir}{Character. Directory where the PNG will be saved. Created if
it does not exist. Default \code{"plots/"}.}

\item{dpi}{Integer. Output resolution for saved PNG. Default \code{400}.}
}
\value{
A \code{tmap} object (invisibly). The PNG is written to disk as a side
effect.
}
\description{
\code{map_cycle_network()} creates a map of cycling infrastructure for a city,
classifies segregated vs non-segregated paths, overlays them on a basemap,
annotates the map with summary statistics (from \code{summarise_cycle_network}) and
saves the result as a PNG. The function returns the \code{tmap} object invisibly.
}
\details{
\strong{Note:} This function relies on helper functions that are not included
here: \code{summarise_cycle_network()}, \code{osmactive::distance_to_road()}, and
\code{osmactive::classify_cycle_infrastructure()}. Ensure these are available in the package
or namespace where you use this function.
}
\examples{
\dontrun{
map_cycle_network("MyCity", my_city_sf, my_osm_data, city_pop = 200000)
}
}
