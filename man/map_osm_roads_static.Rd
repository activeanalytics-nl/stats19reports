% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{map_osm_roads_static}
\alias{map_osm_roads_static}
\title{Static OSM road network casualty map}
\usage{
map_osm_roads_static(
  crashes,
  casualties,
  osm_data,
  casualty_type = NULL,
  year = NULL,
  area_name = NULL,
  city_shape,
  map_title = NULL,
  basemap_bgd_colour = "dark",
  group = "total",
  legend_pos = c(0.03, 0.48),
  top_x = NULL,
  colour_by = c("number_of_collisions", "osm_id", "casualty_type", "age_of_casualty",
    "sex_of_casualty", "speed_limit", "fatal", "serious", "ksi", "slight", "total")
)
}
\arguments{
\item{crashes}{An \code{sf} data frame of crash records.}

\item{casualties}{Casualty data frame.}

\item{osm_data}{An \code{sf} object of OSM driving network segments.}

\item{casualty_type}{Character vector of casualty types to include, or
\code{NULL} for all.}

\item{year}{Integer. Single year to filter by, or \code{NULL} for all.}

\item{area_name}{Character. Area name for the map title. If \code{NULL},
taken from the first crash record.}

\item{city_shape}{An \code{sf} polygon for basemap extent.}

\item{map_title}{Character. Custom title, or \code{NULL} for auto-generated.}

\item{basemap_bgd_colour}{Character. Basemap style: \code{"dark"} or
\code{"light"}.}

\item{group}{Character. Grouping variable passed to
\code{summarise_osm_link_casualties()}. Default \code{"total"}.}

\item{legend_pos}{Numeric vector of length 2 for legend position.}

\item{top_x}{Integer. If not \code{NULL}, show only the top x roads.}

\item{colour_by}{Character. Variable to colour lines by.}
}
\value{
A \code{tmap} object (plot mode).
}
\description{
Creates a static \code{tmap} map of casualty statistics per OSM road link
over a raster basemap. Lines are coloured by a chosen variable and the
result can be filtered by casualty type and/or year.
}
\examples{
\dontrun{
tm <- map_osm_roads_static(crashes, casualties, osm_data = drive_net,
                           colour_by = "ksi", city_shape = city_sf)
}
}
