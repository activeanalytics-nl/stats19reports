% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{summarise_casualties_per_la}
\alias{summarise_casualties_per_la}
\title{Summarise casualties per Local Authority}
\usage{
summarise_casualties_per_la(
  casualties,
  crashes,
  la_geo,
  per_capita = FALSE,
  casualty_types = "All",
  casualty_sexes = c("Male", "Female")
)
}
\arguments{
\item{casualties}{Casualty data frame.}

\item{crashes}{An \code{sf} data frame of crash records.}

\item{la_geo}{An \code{sf} data frame of LA boundaries with
\code{LAD22NM}/\code{LAD22CD} columns.}

\item{per_capita}{Logical. If \code{TRUE}, compute per-1000-population
rates using ONS mid-year estimates. Default \code{FALSE}.}

\item{casualty_types}{Character. Casualty type filter, or \code{"All"} to
include all types. Default \code{"All"}.}

\item{casualty_sexes}{Character vector of sexes to include. Default
\code{c("Male", "Female")}.}
}
\value{
An \code{sf} data frame with one row per LA per year and columns
for casualty counts, rankings, and optionally per-capita rates.
}
\description{
Spatially joins crashes to Local Authority boundaries and aggregates
casualty counts by severity, year, and LA. Adds national rankings and
optionally computes per-capita rates.
}
\examples{
\dontrun{
la_cas <- summarise_casualties_per_la(casualties, crashes)
la_cyc <- summarise_casualties_per_la(casualties, crashes,
                             casualty_types = "Cyclist", per_capita = TRUE)
}
}
