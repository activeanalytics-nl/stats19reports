% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{summarise_vehicles_per_collision}
\alias{summarise_vehicles_per_collision}
\title{Summarise vehicles per collision into one row}
\usage{
summarise_vehicles_per_collision(vehicles, summarise_categories = TRUE)
}
\arguments{
\item{vehicles}{A data frame of vehicle records with \code{collision_index} and
\code{vehicle_type}.}

\item{summarise_categories}{Logical. If \code{TRUE}, vehicle types are simplified
using \code{vehicle_type_lookup}. Default \code{TRUE}.}
}
\value{
A wide-format data frame with one row per collision and counts of
vehicles by type.
}
\description{
Converts a vehicle-level table into a collision-level summary, with one
column per vehicle type and counts as values.
}
\examples{
\dontrun{
veh_wide <- summarise_vehicles_per_collision(vehicles)
}
}
