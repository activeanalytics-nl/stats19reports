% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{st_read_retry}
\alias{st_read_retry}
\title{Read spatial data with retry on failure}
\usage{
st_read_retry(url, wait = 5, max_attempts = 20)
}
\arguments{
\item{url}{Character. URL or file path to read.}

\item{wait}{Numeric. Seconds to wait between retry attempts. Default
\code{5}.}

\item{max_attempts}{Integer. Maximum number of attempts before erroring.
Default \code{20}.}
}
\value{
An \code{sf} object returned by \code{sf::st_read()}.
}
\description{
Wraps \code{sf::st_read()} in a retry loop, useful for unreliable remote
servers (e.g. the ONS Open Geography Portal). Retries up to
\code{max_attempts} times with a configurable wait between attempts.
}
\examples{
\dontrun{
la_sf <- st_read_retry("https://example.com/boundaries.gpkg")
}
}
