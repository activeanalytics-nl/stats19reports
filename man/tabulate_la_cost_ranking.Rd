% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabulate.R
\name{tabulate_la_cost_ranking}
\alias{tabulate_la_cost_ranking}
\title{Create a gt table of Local Authority cost rankings}
\usage{
tabulate_la_cost_ranking(
  crashes,
  severities = c("Fatal", "Serious", "Slight"),
  sort_by = c("casualties", "cost", "collisions"),
  table_year = 2024,
  rows = 10,
  tab_dir = NULL
)
}
\arguments{
\item{crashes}{An \code{sf} data frame of crash records.}

\item{severities}{Character vector of severity levels to include. Default
\code{c("Fatal", "Serious", "Slight")}.}

\item{sort_by}{Character. Column to sort LAs by. One of
\code{"casualties"}, \code{"cost"}, or \code{"collisions"}.}

\item{table_year}{Integer. Year to filter to. Default \code{2024}.}

\item{rows}{Integer. Number of rows (top LAs) to show. Default \code{10}.}

\item{tab_dir}{Character or \code{NULL}. If supplied, a PNG of the table
is also saved into this directory.}
}
\value{
A \code{gt} table object.
}
\description{
Computes collision costs per Local Authority, sorts by the chosen metric,
and produces a styled \code{gt} table of the top \code{n} LAs. The table
is saved to disk.
}
\examples{
\dontrun{
t1 <- tabulate_la_cost_ranking(crashes, sort_by = "cost",
                               table_year = 2024, rows = 10)
}
}
