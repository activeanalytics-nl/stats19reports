% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{casualties_per_MSOA}
\alias{casualties_per_MSOA}
\title{Summarise casualties per MSOA by home location}
\usage{
casualties_per_MSOA(
  casualties,
  per_capita = FALSE,
  by_year = TRUE,
  by_casualty = TRUE,
  casualty_sexes = c("Male", "Female")
)
}
\arguments{
\item{casualties}{Casualty data frame with \code{lsoa_of_casualty}.}

\item{per_capita}{Logical. If \code{TRUE}, compute per-1000-population
rates. Default \code{FALSE}.}

\item{by_year}{Logical. If \code{TRUE}, group by year; otherwise collapse
all years. Default \code{TRUE}.}

\item{by_casualty}{Logical. If \code{TRUE}, group by casualty type;
otherwise collapse to \code{"All"}. Default \code{TRUE}.}

\item{casualty_sexes}{Character vector of sexes to include. Default
\code{c("Male", "Female")}.}
}
\value{
An \code{sf} data frame with one row per MSOA (per year, per
casualty type) and severity counts, rankings, and optionally per-capita
rates.
}
\description{
Matches casualties to MSOAs via their \code{lsoa_of_casualty} home
location, aggregates by severity, casualty type, and year, and adds
rankings. Optionally computes per-capita rates.
}
\examples{
\dontrun{
msoa_cas <- casualties_per_MSOA(casualties)
msoa_cas_yr <- casualties_per_MSOA(casualties, per_capita = TRUE,
                                   by_year = TRUE)
}
}
