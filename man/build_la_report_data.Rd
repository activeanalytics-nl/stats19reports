% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{build_la_report_data}
\alias{build_la_report_data}
\title{Build all data, plots, maps and tables for an LA report}
\usage{
build_la_report_data(
  authority,
  base_year = 2021,
  upper_year = 2025,
  output_dir = file.path("outputs", gsub(" ", "_", authority)),
  imd_url = NULL,
  la_url = NULL,
  pop_url = NULL,
  quick = FALSE
)
}
\arguments{
\item{authority}{Character. Local Authority name (matched against
\code{LAD22NM} with \code{grepl()}), e.g. \code{"Bristol"}.}

\item{base_year, upper_year}{Integers. Analysis period. Note
\code{stats19::get_stats19("5 years")} limits how far back data goes.}

\item{output_dir}{Character. Root output directory. Default
\code{"outputs/<authority>"}.}

\item{imd_url}{Character or \code{NULL}. URL (or local path) of the LSOA
IMD 2025 GeoPackage; \code{NULL} uses the packaged default.}

\item{la_url}{Character. URL of the LA boundaries GeoPackage.}

\item{pop_url}{Character. URL of the LSOA population CSV.}

\item{quick}{Logical. If \code{TRUE}, skips the slow per-street map loops
and national choropleth grid (useful for testing). Default
\code{FALSE}.}
}
\value{
Invisibly, the named list of report objects (also saved as RDS).
}
\description{
Runs the full analysis pipeline for one Local Authority: downloads
STATS19 collision, casualty and vehicle data, computes LA / LSOA / MSOA /
IMD / OSM / speed-limit / pavement / cost summaries, writes all static
plots, interactive maps and tables to \code{output_dir}, and saves the
summary objects needed by the Quarto report template to
\code{output_dir/data/la_report_data.rds}.
}
\examples{
\dontrun{
build_la_report_data("Bristol", base_year = 2021, upper_year = 2025)
}
}
